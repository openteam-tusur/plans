<li>
  <div class="list">
    <h5><%= speciality %></h5>
    <ul>
      <% speciality.actual_subspecialities.each do |subspeciality| %>
        <% if subspeciality.warnings.empty? %>
          <li>
        <% else %>
          <% title = subspeciality.warnings %>
          <li class='warning with_tip' title='<%= title.join(', ') %>'>
        <% end %>
          <div class="wrapper">
            <%= link_to subspeciality, manage_year_speciality_subspeciality_path(@year.number, speciality.code, subspeciality) %>
            <span class='subdepartment'><%= subspeciality.subdepartment.abbr %></span>
            <%= content_tag :div, subspeciality.reduced_text %>
            <%= content_tag :div, subspeciality.kind_text unless subspeciality.kind.basic? %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</li>
